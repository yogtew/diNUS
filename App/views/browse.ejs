<!DOCTYPE html>
<html>
  <head>
		<!-- Page Title -->
    <title>Browse Restaurants</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />

        <!-- CSS for Styling -->
    	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  </head>

  <body>
  	<a href="/">Back</a> &nbsp&nbsp <a href="/browse">All</a><br/>
      <h1>Browse Restaurants</h1>

      <table class="table">
  		<thead>
  		  <tr>
  		    <% for(var i=0; i<fields.length; i++) { %>
  			  <th scope="col"><%= fields[i].name%></th>
  			  <% } %>
  		  </tr>
  		</thead>
  		<tbody>
  		  <% for(var i=0; i<data.length; i++) { %>
  		  <tr>
  		    <% for(var j=0; j<fields.length; j++) { %>
  		    <td><%= data[i][fields[j].name]%></td>
  		    <% } %>
  		  </tr>
  		<% } %>
  	    </tbody>
  	</table>

      <form action="browse" method="post">
        <div>
          <label for="s_rName">Search Restaurant</label>
          <input type="text" class="form-control" id="s_rName" name="s_rName" placeholder="Enter restaurant name to search">
          <button type="submit" class="btn btn-primary">Submit</button>
        </div>
      </form>

  </body>
</html>